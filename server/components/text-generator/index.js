'use strict';

var Promise = require('bluebird');

// After every period + space, question mark, and exclaimation point, capitalize
export function capitalize(bot){
}

// Split at every punctuation mark between a certain distance
export function split(bot){
}

// Prepend sentence to transition into a new topic
export function addTopicTransition(bot){
  return new Promise(function(resolve, reject){
    resolve(bot)
  })
}
