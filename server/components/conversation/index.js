'use strict';

export function respond(){
  // new Bot();
  // Flow.handleAskPermissionResponse();
  // Task.respond();
  // if status == ready, next();
}

export function next(){
  // Strategy.selectTask();
  // Task.setContext();
  // Flow.handleTopicChange();
  // Flow.handleAskPermission();
  // if status == ready, Task.run();
}

export function initiate(){
  // handleNotification();
  // if status == ready, Task.run();
}
