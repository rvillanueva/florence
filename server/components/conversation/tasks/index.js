'use strict';

export function run(){
  // TaskService.send();
  // TaskService.handleWait();
}

export function respond(){
  // TaskService.determineContext();
  // TaskService.handleUserInput();
  // TaskService.fillSlots();
  // TaskService.handleClarification();
  // TaskService.handleCompletion();
}

export function setContext(){
  // TaskService.setContext();
}
