export function getMeasuresNeeded(user, steps) {
  // return steps needed for general check in
  // get most recent logs for user
  // match against tracked measures
}

export function getFlavor(user, steps) {
  // return flavor steps
}

export function updateUserFatigue(user){

}

export function updateUserEngagement(user){

}

export function selectBestStep(user, steps) {
  /*
  select the best step based on
  inherent step-type priority
  user given priority
  freshness
  user fatigue
  user engagement
  current topic
  */
}

export function clarify(user, steps) {
  /*
  select the best step based on
  inherent step-type priority
  user given priority
  freshness
  user fatigue
  user engagement
  current topic
  */
}
